# Mock/demo data package — kept for reference and offline demoing.
# services/backend.py uses real pipeline data by default; nothing here
# is imported unless a caller explicitly reaches into mock.demo_data.
